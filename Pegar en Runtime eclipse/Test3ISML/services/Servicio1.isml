package com.litierses.services ;

/**
 * This service provides the basic methods to access a persistent object storage.
 * In this context, entities are referred as 'objects'.
 */
service Servicio2 < T > {

	native Void remove(T obj);
}